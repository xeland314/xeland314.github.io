---
import PostCover from "../../../layouts/PostCover.astro";

const title = "Standard Streams";
const subtitle = "stdin, stdout & stderr";
const category = "Linux / Unix";
---

<PostCover
  metaTitle="Standard Streams (0, 1, 2) - Visual Guide"
  metaDescription="Radiography of stdin, stdout, and stderr. Why we use 2>&1 to redirect errors"
  lang="en"
  title={title}
  subtitle={subtitle}
  category={category}
  themeColor="orange"
  iconChar="🚿"
/>
